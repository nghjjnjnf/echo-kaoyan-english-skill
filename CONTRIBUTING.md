# 贡献指南

感谢你改进考研英语 skill。这个项目优先接受能提升稳定性、可解释性和可复现性的修改。

## 可以贡献什么

- 阅读、完形、翻译和作文规则的明确修正；
- 新来源 DOCX 的兼容性改进；
- 能复现真实问题的最小测试用例；
- 中文文档、安装说明和错误提示改进；
- 不包含受限正文的评测样例。

## 提交前

1. 新建独立分支。
2. 保持改动范围单一。
3. 不提交真题全文、原始 Word、机构材料或私人数据。
4. 运行以下检查：

```bash
python -m pip install -r requirements-dev.txt
python scripts/validate_repo.py
python -m unittest discover -s tests -v
```

5. 在 Pull Request 中写明问题、修改方式、验证结果和可能影响。

## Skill 修改原则

- `SKILL.md` 只保留任务路由和必要流程。
- 详细题型规则放入 `references/`。
- 重复、确定性的处理逻辑优先放入 `scripts/`。
- 修改回答格式时，同时检查单题和五题批量场景。
- 新增评分规则时，明确英一与英二的分值和题型差异。

## 数据兼容问题

如果导入某份 Word 失败，请提供：

- 已脱敏、且不含真题正文的最小复现 DOCX；
- 文件标题和段落结构说明；
- 运行命令与完整错误信息；
- 预期生成的年份和题型。

不要把完整真题上传到公开 Issue。
